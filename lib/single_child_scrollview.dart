import 'package:flutter/material.dart';

class SingleChildScrolled extends StatelessWidget {
  const SingleChildScrolled({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: const Padding(
            padding: EdgeInsets.all(16.0),
            child: SingleChildScrollView(
              child: Column(
                children: [
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                  TextField(
                      decoration: InputDecoration(
                          hintText: 'hint', labelText: 'label')),
                ],
              ),
            )));
  }
}
